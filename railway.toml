[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node scripts/init-db-if-needed.mjs && next start -H 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
